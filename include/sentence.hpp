#include <iostream>
#include <string>

class Sentence {
private:
    std::string documentID;
    int number;
    // vector<std::string> content;
    
public:
    Sentence();
    ~Sentence();
    // int parse

};

// int parseText(const std::string &path, )